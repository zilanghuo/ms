# Codex Agent Guide

本文件定义 Codex 在本仓库中的通用工作规则。若用户需求与本规则冲突，优先按用户明确要求执行。

## 1. 适用范围

- 适用于本仓库内需求文档、技术方案、接口文档、表结构设计、原型页面及相关说明文档的编写与修改。
- 若仓库内已有正式产物与本规则不一致，优先保持与现有正式内容兼容，不擅自做大范围重构。

## 2. 文档与原型工作

- 涉及 `bi/` 目录下的文档、原型、技术方案、接口文档、表结构设计时，优先保持现有 HTML 单文件风格。
- 涉及 `bi/tkDashboard` 新增或修改原型 HTML 时，默认以 `bi/tkDashboard/销售/销售数据/htmls/商品统计-1.3.html` 作为视觉与交互基准：按钮、颜色、表格、筛选区、卡片、间距、弹窗/抽屉等应尽量沿用该文件的样式口径，除非用户明确指定其他参考。
- `bi/tkDashboard/管理模块` 下的新页面也应优先贴近上述 `商品统计-1.3.html` 风格，不要自行切换为新的主题色、深浅体系或控件风格。
- 修改文档时，标题、章节层级、表格样式、命名口径应尽量与同目录现有文档保持一致。
- 新增栏目、导航或文档链接时，应同步检查首页或入口页是否需要更新。

## 3. 表设计规范

- 只要涉及表结构设计、数仓分层命名、DDL 草案、ODS/DWD/DWS/ADS 表清单、字段命名规范，必须参考 Doris 规范。
- Doris 规范参考文件位置：
  - `/Users/a1/Documents/Office/code/ms-warehouse-batch-data/AGENTS.md`
- 在本仓库进行表设计相关工作时，至少要遵循以下原则：
  - 分层命名遵循 ODS / DWD / DWS / ADS / DIM 约定。
  - 表名、字段名、时间粒度后缀、增长类型后缀优先参考 Doris 规范中的命名说明。
  - 字段命名使用小写加下划线，避免使用 Doris 关键字。
  - 若一个需求与另一个需求的字段体系明显不同，不要为了复用而强行合并到底层同一张 DWD / DWS / ADS 表。
  - 输出 DDL、建表语句、表结构说明时，必须使用 Doris 兼容语法与口径，不要误用 MySQL 专用写法。

## 4. 特殊说明

- 若当前仓库中的业务文档只是参考 Doris 命名规范，而并不直接落 SQL，也要在表结构设计文档里显式说明命名依据。
- 若表设计调整会影响接口文档、技术方案或需求文档中的表名，应同步检查并更新相关引用。
- 若输出的是接口文档，需区分“接口字段命名”和“底表字段命名”两套口径：
  - 接口文档中的请求参数、返回参数一律使用驼峰命名（camelCase）。
  - 原因是接口集成语言为 Java 8，接口入参与出参需符合 Java 常用 DTO / VO 命名风格。
  - 数仓底表、DWD / DWS / ADS / DIM / ODS 表字段仍按 Doris 规范使用小写加下划线命名。
  - 接口文档如需说明底层映射关系，应通过“来源字段 / 对应底表字段”方式说明，不要直接把下划线字段名当成接口返回字段名。

## 5. 上下文恢复文件

- 本仓库使用 `codex-context/` 目录保存账号、实例或任务切换后的恢复上下文。
- 新实例进入本仓库后，应优先读取以下文件：
  - `codex-context/context.md`：当前上下文与重要背景。
  - `codex-context/progress.md`：当前开发进度、已完成事项与阻塞项。
  - `codex-context/decisions.md`：已经确认的技术、文档和流程决策。
  - `codex-context/todo.md`：下一步计划。
  - `codex-context/prompts.md`：常用 Prompt。
- `codex-context/sessions/` 可用于保存按日期或任务拆分的会话摘要。
- 不要在 `codex-context/` 中记录 token、cookie、API key、账号密码或其他敏感凭据。
- 若 `codex-context/` 中的恢复信息与用户最新明确指令冲突，以用户最新明确指令为准。
